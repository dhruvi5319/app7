---
phase: 01-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - index.html
  - vite.config.ts
  - tsconfig.json
  - tsconfig.node.json
  - .eslintrc.cjs
  - .prettierrc
  - src/main.tsx
  - src/App.tsx
  - src/App.module.css
  - src/index.css
autonomous: true

features:
  implements: ["INFRA-SCAFFOLD"]
  depends_on: []
  enables: ["TASK-01", "TASK-02", "TASK-03", "TASK-04", "TASK-05"]

must_haves:
  truths:
    - "`npm run dev` starts Vite dev server and browser shows a React app (even a placeholder)"
    - "`npm run build` exits 0 and produces a `/dist` bundle with no TypeScript errors"
    - "`npm run lint` exits 0 (ESLint passes on all files)"
    - "CSS Modules are supported — a .module.css import resolves without error in dev and build"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "react, react-dom, typescript, vite, vitest"
    - path: "vite.config.ts"
      provides: "Vite build config with React plugin"
      contains: "@vitejs/plugin-react"
    - path: "tsconfig.json"
      provides: "TypeScript strict config"
      contains: "strict"
    - path: "src/App.tsx"
      provides: "Root React component"
      exports: ["App"]
    - path: "src/main.tsx"
      provides: "React DOM entry point"
      contains: "ReactDOM.createRoot"
    - path: ".eslintrc.cjs"
      provides: "ESLint config for TypeScript + React"
      contains: "plugin:react"
    - path: ".prettierrc"
      provides: "Prettier formatting config"
  key_links:
    - from: "src/main.tsx"
      to: "src/App.tsx"
      via: "import App"
      pattern: "import App from"
    - from: "vite.config.ts"
      to: "src/main.tsx"
      via: "entry point resolution"
      pattern: "plugin-react"
---

<objective>
Bootstrap the React + TypeScript + Vite project scaffold with CSS Modules support, ESLint, and Prettier. This establishes the build toolchain and developer experience foundation that every subsequent phase builds upon.

Purpose: A greenfield project needs a working build pipeline before any feature work can begin. This plan gets `npm run dev`, `npm run build`, and `npm run lint` all passing.
Output: A runnable Vite + React + TypeScript project with CSS Modules, ESLint, Prettier, and Vitest configured.
</objective>

<feature_dependencies>
Implements: INFRA-SCAFFOLD: Project scaffold (Vite + React + TypeScript + CSS Modules + ESLint + Prettier + Vitest)
Depends on: None (first phase, no prior work)
Enables: TASK-01: Create task, TASK-02: View tasks, TASK-03: Mark complete, TASK-04: Delete task, TASK-05: Edit task title (all require the scaffold)
</feature_dependencies>

<execution_context>
@/root/.config/opencode/pivota_spec-framework/workflows/execute-plan.md
@/root/.config/opencode/pivota_spec-framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Vite + React + TypeScript project with CSS Modules support</name>
  <files>
    package.json
    index.html
    vite.config.ts
    tsconfig.json
    tsconfig.node.json
    src/main.tsx
    src/App.tsx
    src/App.module.css
    src/index.css
  </files>
  <action>
Create a new Vite project with React + TypeScript from scratch (do NOT use `npm create vite` interactively — write files directly).

**Tech stack (from TechArch):** React 18, TypeScript 5, Vite 5, CSS Modules

**package.json** — create with these exact dependencies:
```json
{
  "name": "task-tracker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.66",
    "@types/react-dom": "^18.2.22",
    "@typescript-eslint/eslint-plugin": "^7.2.0",
    "@typescript-eslint/parser": "^7.2.0",
    "@vitejs/plugin-react": "^4.2.1",
    "eslint": "^8.57.0",
    "eslint-plugin-react-hooks": "^4.6.0",
    "eslint-plugin-react-refresh": "^0.4.6",
    "typescript": "^5.2.2",
    "vite": "^5.2.0",
    "vitest": "^1.4.0",
    "@vitest/ui": "^1.4.0",
    "@testing-library/react": "^14.2.1",
    "@testing-library/jest-dom": "^6.4.2",
    "@testing-library/user-event": "^14.5.2",
    "jsdom": "^24.0.0"
  }
}
```

Then run `npm install` to install dependencies.

**vite.config.ts:**
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  test: {
    globals: true,
    environment: 'jsdom',
    setupFiles: ['./src/setupTests.ts'],
  },
})
```

**tsconfig.json:**
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

**tsconfig.node.json:**
```json
{
  "compilerOptions": {
    "composite": true,
    "skipLibCheck": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowSyntheticDefaultImports": true,
    "strict": true
  },
  "include": ["vite.config.ts"]
}
```

**index.html:**
```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>TaskTracker</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

**src/main.tsx:**
```typescript
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App.tsx'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```

**src/App.tsx:**
```typescript
import styles from './App.module.css'

function App() {
  return (
    <main className={styles.container}>
      <h1>TaskTracker</h1>
      <p>Your task data layer is loading...</p>
    </main>
  )
}

export default App
```

**src/App.module.css:**
```css
.container {
  max-width: 640px;
  margin: 0 auto;
  padding: 2rem 1rem;
}
```

**src/index.css:**
```css
*,
*::before,
*::after {
  box-sizing: border-box;
}

body {
  margin: 0;
  font-family: system-ui, -apple-system, sans-serif;
  font-size: 16px;
  line-height: 1.5;
  background-color: #f9fafb;
  color: #111827;
}
```

Also create **src/setupTests.ts** (needed for Vitest):
```typescript
import '@testing-library/jest-dom'
```

Create placeholder directories to match TechArch structure (so TypeScript can resolve paths):
- `src/components/` (empty — populated in later phases)
- `src/api/` (populated in plan 01-02)
- `src/storage/` (populated in plan 01-02)
- `src/lib/` (populated in plan 01-02)
- `src/types/` (populated in plan 01-02)

Use `mkdir -p` for these. Do NOT create any files inside them yet — that's plan 01-02.
  </action>
  <verify>
```bash
npm install && npm run build 2>&1 | tail -10 && echo "BUILD OK"
npm run dev -- --port 5173 &amp; sleep 3 &amp;&amp; curl -s http://localhost:5173 | grep -q "TaskTracker" &amp;&amp; echo "DEV SERVER OK" &amp;&amp; kill %1
```
  </verify>
  <done>
- `npm install` completes without errors
- `npm run build` exits 0 and produces `/dist` folder with no TypeScript errors
- `npm run dev` starts and the HTML page contains "TaskTracker"
- CSS Modules (.module.css) resolve in both dev and build
- `src/setupTests.ts` exists for Vitest
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure ESLint and Prettier</name>
  <files>
    .eslintrc.cjs
    .prettierrc
    .prettierignore
    .eslintignore
  </files>
  <action>
Set up ESLint for TypeScript + React and Prettier for code formatting.

**.eslintrc.cjs:**
```javascript
module.exports = {
  root: true,
  env: { browser: true, es2020: true },
  extends: [
    'eslint:recommended',
    'plugin:@typescript-eslint/recommended',
    'plugin:react-hooks/recommended',
  ],
  ignorePatterns: ['dist', '.eslintrc.cjs'],
  parser: '@typescript-eslint/parser',
  plugins: ['react-refresh'],
  rules: {
    'react-refresh/only-export-components': [
      'warn',
      { allowConstantExport: true },
    ],
  },
}
```

**.prettierrc:**
```json
{
  "semi": false,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 100
}
```

**.prettierignore:**
```
dist
node_modules
```

**.eslintignore:**
```
dist
node_modules
```

Do NOT install additional eslint plugins — all required plugins are already in devDependencies from Task 1.
  </action>
  <verify>
```bash
npm run lint 2>&1 &amp;&amp; echo "LINT OK"
```
  </verify>
  <done>
- `npm run lint` exits 0 with no errors or warnings on the initial source files
- `.eslintrc.cjs` exists and is valid
- `.prettierrc` exists with the specified config
  </done>
</task>

</tasks>

<verification>
Run all checks to confirm scaffold is healthy:

```bash
# 1. Clean install
npm ci 2>&1 | tail -5 && echo "INSTALL OK"

# 2. TypeScript compile check
npx tsc --noEmit 2>&1 && echo "TSC OK"

# 3. Lint
npm run lint 2>&1 && echo "LINT OK"

# 4. Build
npm run build 2>&1 | tail -10 && echo "BUILD OK"

# 5. Test runner available (no tests yet — should exit 0 or "no tests found")
npm test 2>&1 | tail -5
```
</verification>

<success_criteria>
1. `npm run dev` starts Vite dev server (port 5173) and browser receives valid HTML with "TaskTracker" in the title
2. `npm run build` exits 0 with `/dist` directory created and no TypeScript errors
3. `npm run lint` exits 0 with max-warnings 0
4. CSS Modules resolve correctly (App.module.css imported in App.tsx without error)
5. Vitest is configured in vite.config.ts with jsdom environment and setupFiles pointing to src/setupTests.ts
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` documenting:
- What was created (files list)
- Any version adjustments made during npm install
- Dev server URL confirmed working
- Build output size
</output>
